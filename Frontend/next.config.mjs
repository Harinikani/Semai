/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [],
    unoptimized: false,
  },
  poweredByHeader: false,
  trailingSlash: false,
};

export default nextConfig;